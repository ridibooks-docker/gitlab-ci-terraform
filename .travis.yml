sudo: required

language: generic

branches:
  only:
  - master

services:
  - docker

env:
  - TERRAFORM_VERSION=0.11.13
  - TERRAFORM_VERSION=0.11.14
  - TERRAFORM_VERSION=0.12.2
  - TERRAFORM_VERSION=0.12.3

script:
  - bin/build.sh
  - bin/push.sh
